{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Hushmark window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show", "dialog:allow-open"]
}
